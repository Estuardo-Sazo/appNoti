import { ImageReportPipe } from './image-report.pipe';

describe('ImageReportPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageReportPipe();
    expect(pipe).toBeTruthy();
  });
});
